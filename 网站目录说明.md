# 个人网站目录说明

> 最后更新：2026-07-22
> 修改网站时先看这个文件，快速定位要改的文件。

## 网站代码（根目录，GitHub Pages 直接发布）

| 文件/文件夹 | 说明 |
|---|---|
| `index.html` | **主站首页**，所有页面和功能都在这一个文件里 |
| `CNAME` | GitHub Pages 域名配置（qhwk2016.com） |
| `police-icon.png` | 公安备案图标（footer 引用） |
| `五维评价/` | 多维评价子系统（class-dashboard.html） |
| `作业批改系统/` | 作业批改子系统（index.html） |
| `课堂行为/` | 课堂行为展示页（静态 HTML，从 Flask 改造） |
| `qhwk-api/` | 后端 API 代码（FastAPI + SQLite，已部署到服务器） |
| `质量平台/` | 质量分析平台（本地运行，双击 start_quality.bat 启动） |

## 资料文件夹（gitignored，不上传 GitHub）

| 文件夹 | 内容 |
|---|---|
| `备案资料/` | ICP 备案 + 公安联网备案全部文件（流程、数据码、身份证复印件等） |
| `部署文档/` | 服务器信息、阿里云购买/部署指南、部署截图记录 |
| `数据与账号/` | 网站数据备份 JSON、GitHub Token、登录密码 |
| `参考资料/` | 原始项目文件和开发素材，含以下子文件夹： |
| `参考资料/教学质量可视化平台-原始文件/` | 质量平台原始 HTML（335KB 硬编码数据）、data.json、CSV 数据、生成/美化脚本（从 F 盘迁入） |
| `参考资料/课堂行为检测系统-源码/` | 课堂行为检测系统 Flask 源码 + YOLO 模型文件（.pt），不含环境安装包（从 F 盘迁入） |
| `参考资料/AI提示词/` | AI 提示词、系统设计构思文档 |
| `课堂行为评价系统/` | 课堂行为的 Flask 原始项目源码（Python，含 AI 检测） |

## 常用操作速查

### 改主站内容
编辑 `index.html`，改完推送到 GitHub：
```bash
cd D:/个人网站
git add index.html
git commit -m "更新说明"
git push
```
如果 `git push` 被热点拦截，用 GitHub REST API 上传（参考 `数据与账号/Token.docx` 里的 Token）。

### 改后端 API
编辑 `qhwk-api/` 里的文件，本地测试后推送到 GitHub 私有仓库，服务器 `git pull`。

### 启动质量分析平台
双击 `质量平台/start_quality.bat`，浏览器自动打开 localhost:8765，用 admin/admin123 登录。

### 域名/备案信息
- 主域名：qhwk2016.com
- API 域名：api.qhwk2016.com
- 中文域名：青花微课.com（跳转到主域名）
- ICP 备案号：粤ICP备2026098482号-1
- 公安备案号：粤公网安备44198202000238号
- 服务器 IP：43.139.55.178（腾讯云 Lighthouse）

## 系统文件（不用管）

- `.git/` — Git 仓库
- `.gitignore` — Git 忽略规则
- `.workbuddy/` — WorkBuddy 工作目录
